import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexGrow: 1,
    padding: 20,
    backgroundColor: '#EBF1F5',
  },
  Text: {
    marginBottom: 10,
  },
});

export default styles;
